# noldor — Long-horizon backlog

**Abstract destinations**, not steps. Items get decomposed into `queue.md` one at
a time as they become unblocked. See `CLAUDE.md` § "Queue and longer-horizon work".

---

## Site content

- **Grow the site as direction firms up.** Beyond the minimal landing: deeper
  product explanation, who it's for. Stay practical, not lore-heavy
  (`data_lake/notes.md`). Keep the clean minimal aesthetic — the celestial/dark
  "shared identity" theme was tried and reverted as too garish (2026-06-17).

## Brand / cross-site

- **`sutra.noldor.tech`.** A `sutra.yantraos.org-redirect` already points at
  `sutra.noldor.tech`; serving that subdomain is a Sutra-repo concern, noted here
  for coordination.

## Logo polish

- **Possible exact-Telcontar wordmark.** Current wordmark is a faithful trace of
  the tecendil PNG (IoU 0.987). A glyph-perfect Telcontar build would need a
  shaping engine (HarfBuzz/Graphite) the current tooling lacks.

## Reverted / not pursuing

- ~~Common visual identity with Sutra via Yantra's `identity.css` + `celestial.css`~~
  — adopted then **reverted 2026-06-17**: the user found the dark/celestial theme
  too garish and chose the earlier clean off-white minimal landing. Do not
  re-introduce the celestial/identity theme without explicit direction.
